# Copy this file to .env.remote, then replace the example value with the
# CloudFront frontend URL printed by the infrastructure deployment.
VITE_API_PROXY_TARGET=https://your-distribution.cloudfront.net
